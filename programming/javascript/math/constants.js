console.log(Math.PI);       // 3.142 (Pi)
console.log(Math.E);        // 2.718 (Euler's number)
console.log(Math.SQRT2);    // √2 ≈ 1.414
console.log(Math.SQRT1_2);  // √(1/2) ≈ 0.707
console.log(Math.LN2);      // ln(2)
console.log(Math.LN10);     // ln(10)
console.log(Math.LOG2E);    // log2(e)
console.log(Math.LOG10E);   // log10(e)

console.log(Number.EPSILON);            // Difference between 1 and smallest number smaller than 1
console.log(Number.MAX_VALUE);          // Greatest possible number in JavaScript
console.log(Number.MIN_VALUE);          // Smallest possible number in Javascript
console.log(Number.MAX_SAFE_INTEGER);   // Maximum safe integer (2^53 - 1)
console.log(Number.MIN_SAFE_INTEGER);   // Minimum safe integer -(2^53 - 1)
console.log(Number.POSITIVE_INFINITY);  // Infinity (returned on overflow)
console.log(Number.NEGATIVE_INFINITY);  // Negative infinity (returned on overflow)
console.log(NaN);                       // A Not-a-Number value
